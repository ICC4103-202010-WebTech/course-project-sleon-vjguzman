<p id="notice"><%= notice %></p>
<%= stylesheet_link_tag 'event-comments'%>
<h2><%= Event.select(:description).where(id: @comments.first.event_id).first.description%> comments:</h2>
<aside class="aside-comment">
<button class="btn11 success-1" ><%= link_to 'New Comment', new_comment_path %></button>
<button class="btn11 info-1" ><%= link_to 'Back', events_path %></button>
</aside>

  <tbody>
  <% @comments.each do |comment| %>
    <div>
      <h4 class="membersh4-1"><%= link_to image_tag("xd.jpeg") %></h4>
      <h4 class="User"><%= User.select(:username).where(id: comment.user_id).first.username%>:</h4>
      <h4 class="User"><%= comment.message %></h4>
      <button class="btn1-1 danger"><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></button>
    </div>
  <% end %>
  </tbody>|
</table>

<br/>

