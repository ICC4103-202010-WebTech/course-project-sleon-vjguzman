<p id="notice"><%= notice %></p>
<%= stylesheet_link_tag 'seedetails-profile'%>
<h3 align="center"> <%= @first_user.full_name %> </h3>
  <div>
    <h4> Email: <%= @first_user.email %></h4>
    <h4> Password: <%= @first_user.password %></h4>
    <button><%= link_to 'Edit', edit_user_path(@first_user) %></button>
  </div>
<br/>
<button class="back"><%= link_to 'Back', users_path %></button>
