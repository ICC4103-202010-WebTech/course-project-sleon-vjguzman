<h1>New Search</h1>
<%= form_for @search do |f| %>
  <div class="field">
    <%= f.label :keywords %><br/>
    <%= f.text_field :keywords %>
  </div>
  <div class="actions">
    <%= f.submit "Search" %>
  </div>
<%end%>

<%= render 'form', search: @search %>

<%= link_to 'Back', searches_path %>
