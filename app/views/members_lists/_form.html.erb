<%= form_with(model: members_list, local: true) do |form| %>
  <% if members_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(members_list.errors.count, "error") %> prohibited this members_list from being saved:</h2>

      <ul>
        <% members_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
