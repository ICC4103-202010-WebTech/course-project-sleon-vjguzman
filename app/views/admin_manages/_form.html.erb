<%= form_with(model: admin_manage, local: true) do |form| %>
  <% if admin_manage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_manage.errors.count, "error") %> prohibited this admin_manage from being saved:</h2>

      <ul>
        <% admin_manage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
